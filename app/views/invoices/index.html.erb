<% content_for :sidebar_footer do %>
  <%= link_to "Create New Invoice", new_invoice_path,
      class: "font-semibold pb-1 border-b-2 border-transparent hover:border-white transition-colors duration-300" %>
<% end %>

<div class="max-w-4xl mx-auto bg-white p-6 rounded shadow-sm">
  <h1 class="font-bold text-3xl text-[#373754] mb-4">Your Invoices</h1>
  
  <div class="space-y-4">
    <%= render partial: "invoice_card", collection: @invoices, as: :invoice %>
  </div>
</div>